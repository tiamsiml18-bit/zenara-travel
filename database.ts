/**
 * Placeholder Supabase types.
 *
 * Once the Supabase project is created and migration 0001_init.sql has been
 * applied, replace this entire file with generated types:
 *
 *   npx supabase gen types typescript --project-id <project-ref> > types/database.ts
 *
 * Until then this loose type keeps the app compiling; it intentionally does
 * NOT type-check column names, so don't leave it in place past initial setup.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
